<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="buttonCalculator">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">@dimen/buttonHeight</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textSize">@dimen/textSizeNumbers</item>
    </style>

    <style name="numberCalculator" parent="buttonCalculator">
        <item name="android:background">@drawable/background_number</item>
    </style>

    <style name="operationCalculator" parent="buttonCalculator">
        <item name="android:background">@drawable/background_operation</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>
